from django.apps import AppConfig
# Hybrid_Trading app config
class HybridTradingConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'Hybrid_Trading'
    label = 'hybrid_trading_app'  # Unique label for the Hybrid_Trading app
